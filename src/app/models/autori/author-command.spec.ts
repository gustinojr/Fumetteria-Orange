import { AuthorCommand } from './author-command';

describe('UserCommand', () => {
  it('should create an instance', () => {
    expect(new AuthorCommand()).toBeTruthy();
  });
});

describe('UtenteCommand', () => {
  it('should create an instance', () => {
    expect(new AuthorCommand()).toBeTruthy();
  });
});