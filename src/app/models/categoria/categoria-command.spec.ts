import { CategoriaCommand } from './categoria-command';

describe('CategoriaCommand', () => {
  it('should create an instance', () => {
    expect(new CategoriaCommand()).toBeTruthy();
  });
});

describe('UpdateCategoriaCommand', () => {
  it('should create an instance', () => {
    expect(new CategoriaCommand()).toBeTruthy();
  });
});