import { CategoriaDTO, CategoriaListDTO } from './categoria-dto';

describe('CategoriaListDTO', () => {
  it('should create an instance', () => {
    expect(new CategoriaListDTO()).toBeTruthy();
  });
});

describe('CategoriaListDTO', () => {
  it('should create an instance', () => {
    expect(new CategoriaDTO()).toBeTruthy();
  });
});